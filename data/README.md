Data folder
===========

Structure:
- data/raw      : raw synthetic sessions (jsonl)
- data/processed: cleaned/extracted signals

Do not commit large raw datasets to the repository. Keep synthetic data small
for tests; store larger generated sets externally if needed.
